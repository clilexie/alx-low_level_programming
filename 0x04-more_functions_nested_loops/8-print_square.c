#include "main.h"

/**
 * print_square - prints a square of size
 * @size: size of he square
 *
 * Return: void
 */

void print_square(int size)
{
	int i, j;
	if (size <= o)
		_putchar('\n');
	for (i=0; i < size; i++)
	{
		for (j = 0; j < (size); j++)
		{			_putchar('#')
		}
	}
	_putchar('\n')
}
